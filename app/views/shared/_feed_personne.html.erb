<% if @feed_items_personne.any? %>
  <ol class="personnes">
    <%= render partial: 'shared/feed_item_personne', collection: @feed_items_personne %>
  </ol>
  <%= will_paginate @feed_items_personne %>
<% end %>
